<view class="">
  <web-view src="{{src}}"></web-view>

   <!-- <scroll-view scroll-x class="bg-white nav text-center fixed" style="top:{{CustomBar}}px">
      <view class="cu-item {{index==TabCur?'text-blue cur':''}}" wx:for="{{cateList}}" wx:key bindtap='tabSelect'
        data-id="{{index}}">
        {{item.name}}
      </view>
    </scroll-view> -->
    
     <!-- <view class="cu-list  menu " style="margin-top:50px">
       <view class="cu-item  " wx:for="{{articleList}}" wx:key="index" >
         <view class="content padding text-left">
           <view> {{item.title}}</view>
           <view class="text-gray text-sm text-right">
             {{item.create_time}}
           </view>
         </view>
       </view>
     </view> -->
     
     
   </view>